#!/usr/bin/env bash
# print the remaining charge on a macbook

pmset -g batt |
  tr "\n" ";" |
  sed -e "s/^Now drawing from 'AC Power'//" \
      -e "s/Now drawing from 'Battery Power'/⚛/" |
  awk '{print $1 " " $3}'|tr -d ";"
