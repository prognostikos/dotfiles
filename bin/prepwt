#!/bin/bash

set -euo pipefail

if command -v direnv > /dev/null; then
  direnv allow
fi

fixwt

[ -f script/update ] && script/update && [ -f db/structure.sql ] && git checkout db/structure.sql

echo "✓ Worktree prepped for work"
